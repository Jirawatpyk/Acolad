# Pre-Implementation Checklist: ระบบ Login และตรวจจับงานใหม่บน Acolad Partner Portal

**Purpose**: "Unit tests สำหรับ requirements" — ตรวจคุณภาพ ความชัดเจน และความครบถ้วน
ของข้อกำหนดใน 4 ด้าน (Reliability & Recovery + Idempotency, Detection
Correctness, Security & Credentials, Ops Readiness) ก่อนเข้าสู่ `/speckit-tasks`
และการ implement — ใช้โดยเจ้าของโปรเจกต์ + Claude
**Created**: 2026-06-10
**Evaluated**: 2026-06-10 — ประเมินโดย workflow 5 agents (pass 1 / partial 19 /
gap 9) แล้วแก้เอกสารครบทุกข้อ; สถานะด้านล่างคือหลังการแก้
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.

## Reliability & Recovery

- [x] CHK001 มี requirement กำหนดพฤติกรรมเมื่อฐานสถานะ (SQLite) เสียหาย
  เปิดไม่ได้ หรือ migrate ไม่สำเร็จหรือไม่? [Gap → แก้แล้ว: เพิ่ม FR-017
  (เก็บสำเนา + alert + cold start) และ quickstart V12]
- [x] CHK002 มี requirement กำหนดพฤติกรรมเมื่อไฟล์ session เสียหายหรือถูกลบ
  หรือไม่? [Partial → แก้แล้ว: ขยาย FR-002 ครอบไฟล์สูญหาย/เสียหาย/อ่านไม่ได้
  — ห้าม crash loop]
- [x] CHK003 เกณฑ์ "ฟื้นตัวภายใน 2 นาที" ระบุจุดเริ่มนับเวลาชัดหรือไม่?
  [Partial → แก้แล้ว: SC-006 นิยามนับจาก process start → poll แรกสำเร็จ,
  ไม่รวมเวลาบูต OS แต่ทั้งหมดต้องอัตโนมัติ]
- [x] CHK004 มี requirement กำหนดสิ่งที่เกิดเมื่อการแจ้งเตือนล้มเหลวถาวรหรือไม่?
  [Partial → แก้แล้ว: เพิ่ม FR-018 (cap 10 ครั้ง/6 ชม. + ping /fail ผ่าน
  Healthchecks ใน 15 นาที) + ตัวแปร OUTBOX_* ใน config contract]
- [x] CHK005 ข้อสมมติเรื่องบริการเฝ้าระวังภายนอกถูกบันทึกและตรวจสอบแล้ว
  หรือไม่ — มีแผนสำรอง? [แก้ครบแล้ว 2026-06-10: แผนสำรองบันทึกใน research
  R5, ผู้ใช้สร้าง check (period 5/grace 5) + ผูก Google Chat integration +
  ทดสอบตามขั้นตอนแล้ว, ping URL ใน .env ยืนยันได้ผล `OK` — การยืนยันซ้ำ
  end-to-end จะทำอีกครั้งใน T047 ก่อนรัน 24/7]
- [x] CHK006 มี requirement ครอบคลุมการรีบูตอัตโนมัติของ Windows Update
  หรือไม่? [Partial → แก้แล้ว: เพิ่ม Assumption + ขั้นตอนใน quickstart
  (auto-logon+lock แนะนำ หรือ NSSM) + ข้อจำกัด pm2-windows-startup
  บันทึกใน research R6]

## Idempotency & State

- [x] CHK007 นิยาม "งานหายไปจากรายการ" ระบุจำนวนรอบตรวจขั้นต่ำใน spec
  หรือไม่? [Partial → แก้แล้ว: FR-007 ระบุ ≥ 2 รอบตรวจติดต่อกัน]
- [x] CHK008 spec กำหนดพฤติกรรมเมื่อรายละเอียดงานเดิมเปลี่ยนหรือไม่?
  [Gap → แก้แล้ว: เพิ่ม FR-019 — อัปเดต+บันทึกเงียบ ไม่แจ้งเตือน
  (**product decision โดย default** — เปลี่ยนเป็นแจ้งได้ถ้าทีมต้องการ)]
- [x] CHK009 มี requirement สำหรับ cold start เกิดซ้ำหรือไม่? [Gap → แก้แล้ว:
  FR-015 เพิ่มกติกาแจ้งเตือนเมื่อเกิดครั้งที่ 2 ใน 7 วัน]
- [x] CHK010 retention ของข้อมูลถูกกำหนดหรือไม่? [Partial → แก้แล้ว: เพิ่ม
  หัวข้อ Data Retention ใน data-model (sent 90 วัน, dead จนเคลียร์,
  evidence ≥ 90 วัน)]
- [x] CHK011 กติกา job key fallback มี requirement ระดับ spec หรือไม่?
  [Partial → แก้แล้ว: FR-005 ระบุฟิลด์ identity + data-model ระบุกติกา
  serialize null แบบ deterministic]
- [x] CHK012 นิยาม "การปรากฏครั้งเดียวกัน" ชัดพอเขียน test ได้หรือไม่?
  [Partial → แก้แล้ว: FR-005 นิยามครบ เริ่ม-สิ้นสุด + กรณีรีสตาร์ต]

## Detection Correctness

- [x] CHK013 เกณฑ์ "ตรวจพบภายใน 30 วินาที" วัดอย่างเป็นภววิสัยได้หรือไม่?
  [Partial → แก้แล้ว: SC-001 นิยามวัดสองชั้น (fixture / poll-gap proxy)]
- [x] CHK014 มี requirement ครอบ pagination หรือไม่? [Gap → แก้แล้ว:
  Assumption "หน้าเดียว" + FR-009 บังคับ alert เมื่อพบตัวบ่งชี้หลายหน้า
  (**ห้ามทำงานต่อเงียบๆ**)]
- [x] CHK015 เกณฑ์แยก "ว่างจริง" vs "อ่านไม่สำเร็จ" อยู่ระดับ requirement
  หรือไม่? [Partial → แก้แล้ว: เพิ่ม FR-016 (marker บังคับ)]
- [x] CHK016 พฤติกรรมเมื่อ portal เปลี่ยนภาษา/locale ถูกกำหนดหรือไม่?
  [Gap → แก้แล้ว: Assumption ภาษาเดียว + FR-009 นับ locale เปลี่ยน
  เป็นกรณีหน้าอ่านไม่ได้]
- [x] CHK017 ขอบเขต "งานที่บัญชีมองเห็น" นิยามชัดหรือไม่? [Partial → แก้แล้ว:
  Assumption ระบุ = ทุกแถวในหน้ารายการที่เฝ้า ไม่กรองสถานะกดรับ,
  แท็บอื่นนอกขอบเขต, ทบทวนเมื่อเห็นหน้าจริง]
- [x] CHK018 เพดาน burst ระบุเป็นตัวเลขผูกพันหรือไม่? [Partial → แก้แล้ว:
  เพิ่ม SC-008 (25 งาน/รอบ, ส่งแยกงานละ 1 ข้อความเสมอ — การรวบชุดถูกตัด
  ออกตามผล /speckit-analyze, เกิน 25 ห้ามสูญเสียเหตุการณ์)]

## Security & Credentials

- [x] CHK019 รายการ secrets ที่ต้อง redact ระบุครบและวัดได้หรือไม่?
  [Partial → แก้แล้ว: FR-012 enumerate 5 รายการ + เกณฑ์ตรวจรับแบบสแกน
  เทียบค่า config; รายการ sync ใน config contract]
- [x] CHK020 Healthchecks ping URL ถูกจัดประเภทเป็น secret หรือไม่?
  [Gap → แก้แล้ว: FR-012 ข้อ (5) + คำเตือนใน config contract และ
  .env.example]
- [x] CHK021 ไฟล์ session ถูกคุ้มครองระดับ requirement หรือไม่? [Partial →
  แก้แล้ว: FR-012 ยกระดับ session/evidence เท่ารหัสผ่าน + quickstart
  Prerequisite ข้อ 5 (ตรวจ .gitignore + ล็อกหน้าจอ)]
- [x] CHK022 พฤติกรรมเมื่อ webhook ถูก revoke ถาวรถูกกำหนดหรือไม่?
  [Gap → แก้แล้ว: FR-018 แยกถาวร/ชั่วคราว + ตารางจำแนก HTTP code ใน
  notifications contract + quickstart V13]
- [x] CHK023 การลบ docs/Auth.txt ถูกบันทึกปิดงานหรือไม่? [Partial → แก้แล้ว:
  ตรวจพบว่าไฟล์**ถูกลบแล้วจริง** (docs/ ว่าง) — อัปเดต constitution v1.0.1
  บันทึก RESOLVED + quickstart เพิ่มข้อตรวจซ้ำก่อน git init]

## Ops Readiness

- [x] CHK024 ทุก SC มีผู้รับผิดชอบและวิธีบันทึกผลหรือไม่? [Partial → แก้แล้ว:
  เพิ่มตารางผู้รับผิดชอบ + แหล่งวัด + ที่บันทึก (docs/acceptance/001.md)
  ใน quickstart]
- [x] CHK025 SC-007 มี validation scenario รองรับหรือไม่? [Gap → แก้แล้ว:
  เพิ่ม V14 (30 วัน: uptime + อัตราเรียก + login ยืนยันบัญชี) + เกณฑ์ผ่าน
  ขั้นสุดท้ายครอบ SC-004/SC-007]
- [x] CHK026 ข้อความ "ต้องทำ" ของ SYSTEM_ALERT นิยามครบทุก trigger หรือไม่?
  [Partial → แก้แล้ว: เพิ่มตาราง action 8 trigger ใน notifications contract]
- [x] CHK027 วิธีวัด uptime 99% ระบุชัดหรือไม่? [Partial → แก้แล้ว: SC-004
  นิยาม downtime = ช่องว่าง ping > 10 นาที + browser recycle ไม่นับ]
- [x] CHK028 ขั้นตอนอัปเดต dependencies ระยะยาวถูกกำหนดหรือไม่? [Gap →
  แก้แล้ว: Assumption ประกาศนอกขอบเขต + หัวข้อ Maintenance ใน quickstart
  (4 ขั้นตอนปลอดภัย)]
- [x] CHK029 เงื่อนไขก่อนรันจริง (HEALTHCHECKS_PING_URL) ถูกระบุเป็น blocking
  dependency หรือไม่? [Pass ตั้งแต่แรก: quickstart Prerequisites + fail-fast
  ใน config contract — ค่าจริงตั้งแล้วและ ping ทดสอบได้ `OK` (ดู CHK005)]

## Notes

- Check items off as completed: `[x]`
- สถานะ **29/29 ผ่านครบ** (2026-06-10) — CHK005 ปิดหลังผู้ใช้ตั้งค่า
  Healthchecks.io ครบและ ping ทดสอบได้ `OK` (บันทึกใน research R5)
- การตัดสินใจเชิง product ที่ใช้ค่า default (ทีม override ได้): CHK008
  (งานแก้รายละเอียด = บันทึกเงียบ), CHK014 (สมมติหน้าเดียว + alert เมื่อพบ
  pagination), CHK017 (แจ้งทุกแถวรวมงานที่กดรับไม่ได้แล้ว)
- Items are numbered sequentially for easy reference
